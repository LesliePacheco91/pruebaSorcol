const {storageModel} = require('../models');

const createStorage = (req, res) =>{

    const {body} = req
    console.log(body);
}

const updateItem = (req, res) =>{}

const deleteItem = (req, res) =>{}

module.exports = {createStorage } // exportacion desestructurada

